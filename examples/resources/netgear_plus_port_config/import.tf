# The resource ID is "nsdp@" followed by the switch's agent MAC address
# (the same value as the provider's agent_mac attribute).
terraform import netgear_plus_port_config.switch nsdp@8c:3b:ad:25:1b:88
